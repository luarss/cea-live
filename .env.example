# Environment Variables for CEA-VIZ Backend

# Port for the Express server (default: 3003)
# In production on Render, this is set automatically
PORT=3003

# Node environment (development | production)
NODE_ENV=development

# Frontend origin URL for CORS configuration
# This restricts API access to only your frontend domain
# Multiple origins are allowed by default for local development:
#   - http://localhost:3000 (React default)
#   - http://localhost:5173 (Vite default)
#   - http://localhost:3003 (Same origin - backend serving frontend)
#
# For production deployment:
# Set this to your deployed frontend URL, e.g.:
# FRONTEND_ORIGIN=https://your-app-name.onrender.com
#
# Leave blank for local development to use defaults
FRONTEND_ORIGIN=
