services:
  - type: web
    name: cea-viz
    env: node
    region: singapore
    plan: free
    buildCommand: make install-lfs && make build
    startCommand: make start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        sync: false
